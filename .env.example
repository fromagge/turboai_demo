DEBUG=True
SECRET_KEY=change-me-to-a-real-secret-key
ALLOWED_HOSTS=localhost,127.0.0.1,web

# Postgres (used by compose for postgres service; web uses PGHOST=postgres in compose)
PGHOST=localhost
PGPORT=5432
PGDATABASE=turboai
PGUSER=turboai
PGPASSWORD=turboai

# Redis (web uses REDIS_HOST=redis in compose)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
